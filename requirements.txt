pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
joblib>=1.3
matplotlib>=3.7
streamlit>=1.30
plotly>=5.18

# Optional — needed only if you extend the modeling stage with the
# time-series / gradient-boosting methods described in the research paper
# but not executed in this build (no internet access in the build sandbox):
# statsmodels        # ARIMA / SARIMA
# prophet            # Facebook Prophet
# xgboost            # XGBoost regressor
